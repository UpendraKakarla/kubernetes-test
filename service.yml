apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    matchLabels:
      app: nginx
    
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
